import styled from "styled-components";

const Wrapper = styled.div`
  max-width: 450px;
  margin-top: 5%;
`;

const Form = styled.form`
  display: flex;
  align-items: center;
  gap: 10px;
`;

export { Wrapper, Form };
